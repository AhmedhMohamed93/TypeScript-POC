function Add(FirstNum: number,SecondNum: number): number
{
    return FirstNum + SecondNum;
}

console.log(Add(10,15));
console.log(Add('a',15)); // Can't use the "Add" function as the given arguments are not compatible with the defined arguments data typs.